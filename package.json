{
  "name": "demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest --watchAll --verbose --coverage"
  },
  "devDependencies": {
    "jest": "^29.3.1",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "@types/jest": "^29.2.4"
  }
}
